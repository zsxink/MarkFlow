# P3 验证记录：默认编辑路径

总体状态：IN PROGRESS（P2 已 GO；P3 start identity 已记录，任务 5.1–5.13 未完成，default-on 未获准发布）

正式设计：[P3：默认编辑路径](../../design/phases/P3-default-editing-path.md)

## Candidate identity

| Branch | Commit | Default flags | Run ID |
| --- | --- | --- | --- |
| `test/issue-255-lossless-byte-contract` | `95f215b`（P3 start HEAD；非冻结候选） | `losslessCoreSession`=ON（未验收）、`codemirrorLivePreview`=ON（未验收） | `20260820-p3-start-95f215b` |

> ⚠️ P3 门禁说明（task 指令 §8）：`7ff9da1` 已提前默认开启 lossless Core/Live
> Preview，但 tasks 5.1–5.13、minimum parity、P3 Reviewer 和人工验收均未完成。
> 该默认入口必须视为「未验收候选实现」；在 5.10 minimum parity 逐行通过并由
> Program Owner 签署前，默认入口不得被视为已获准发布。当前阶段目标是把门禁恢复为
> 「未获准即不宣布发布」的最小、安全、可回滚状态，并保留显式 opt-out / legacy
> rollback。

## 开始前现有实现 gap audit（2026-08-20）

对现有 `commandRouter.ts` / `toolbar.ts` / `keyboard.ts` / `editorSurfaceBinding.ts` /
`integration.ts` / `sourceSyncController.ts` / `losslessSourceEditor.ts` / `editor.ts` /
`sidebar.fileops.ts` / `imageUtils.ts` / `exportLossless.ts` 做了 gap audit，结论：

**已有（可在其上继续沉淀，不盲目重写）：**

- 统一 command router 雏形（`commandRouter.ts`）：`toggleInlineWrap`（bold/italic/strike/code）、
  `toggleHeading`（仅 level 1/2）、`toggleQuote`、`toggleList`（ul/ol）、`toggleCodeBlock`、
  `insertHorizontalRule`、`insertLinkMarkdown`、`insertImageMarkdown`，均为局部 CM transaction。
- toolbar/menu/keyboard/linkDialog 已路由到 active lossless view。
- paste 原始 EOL provenance 捕获（`onRawPasteText` → `pendingRawPasteProvenance`）。
- 图片保存前迁移为显式局部 patch（`preparePendingImagesForSave` → `applyImagePatches`）。
- 保存/autosave 走 lossless confirmed snapshot（`binding.save`），export 走 `buildLosslessExportRoot`。
- Lifecycle 自动化已在 `lifecycle.test.ts` 中覆盖零编辑、autosave tick、干净 Ctrl+S。

**缺口（本 Slice 必须补齐）：**

1. **Enter/Backspace/Delete command matrix 完全缺失**（任务 5.4）——lossless 下只用 CM
   `basicSetup` 的普通文本语义，无 markdown block-continuation / 空 list item 退出 /
   heading marker 退级等。这是默认编辑路径的最大 gap。
2. **命令测试覆盖面不足**（任务 5.2/5.4）：当前 `commandRouter.test.ts` 只有 collapsed
   selection 为主的 10 项；缺少 range/reversed selection、行首尾、嵌套、CJK/emoji、
   malformed fallback、Undo/Redo 的 table-driven 全覆盖。
3. **toolbar 命令在 Live Preview mode 下的 active binding 检查存在 P1 遗留**（P2 Reviewer
   已归口 P3 tasks 5.1-5.3）——需要确认预览 mode 下所有命令仍路由到同一 EditorView。
4. **Heading 命令只有 level 1/2**（P2 观察项 P2-A），与 toolbar `btn-h1/btn-h2` 一致；
   任务 5.2 要求 heading 命令迁移完整。
5. **Image 替换/删除入口缺失**：只有 toolbar `btn-image` 的插入；`onReplace` 钩子存在但
   当前没有被 WYSIWYG 或 lossless 的 context menu 调用（imageContextMenu 只有复制/另存为/
   复制路径/打开所在位置）。任务 5.3 要求图片插入/替换/删除 + 资源事务。
6. **History 显式 grouping 未实现**（任务 5.6）：未为 paste/Enter/结构命令/widget commit/
   bulk replace 显式加 History boundary；跨模式 Undo/Redo 只有零散依赖 basicSetup。
7. **P4 边界**：marker 隐藏（P4A/B）、task checkbox、image/GFM table widget、FrontMatter/
   Mermaid/PlantUML widget、Core Render IR 均不实现；table/task/frontmatter/reference/
   footnote/diagram/raw HTML 在本段为 exact source fallback（必须可读、可选、可编辑、
   可复制、可保存）。

## AI Coding 验证

- [ ] 通用全 gate
- [ ] toolbar/menu/shortcut command parity
- [ ] Enter matrix：paragraph/heading/list/quote/fence/table fallback/cross-block
- [ ] Backspace/Delete matrix：marker/block/atomic/image/malformed
- [ ] paste：literal/plain/HTML/file/image/async stale
- [ ] image resource prepare/commit/compensation
- [ ] History grouping：typing/IME/paste/Enter/widget/bulk
- [ ] 跨 Source/Live Preview Undo/Redo
- [ ] autosave/conflict/reload/close/save-as/new
- [ ] export 不修改正文/dirty
- [ ] A/B command/History/resource 隔离
- [ ] canonical full workflow bytes
- [ ] 默认 flags 零编辑 lifecycle + 两个 autosave tick + 干净 Ctrl+S + 关闭/reopen
- [ ] default/legacy owner isolation
- [ ] minimum parity matrix：基础语义、commands、image fallback、复杂 construct fallback、selection/IME、data/save
- [ ] rollback 演练

## 人工验证记录

- 验收人/环境：NOT RECORDED
- [ ] 新建并输入中文标题/段落/list/quote/code
- [ ] 工具栏与快捷键
- [ ] 空/非空/嵌套 list Enter/Backspace
- [ ] 粘贴网页/纯文本/代码/图片
- [ ] 图片修改删除与 Undo/Redo
- [ ] 跨模式继续 Undo/Redo
- [ ] autosave + external conflict
- [ ] Save As/关闭重开/export
- [ ] A/B 双文档
- [ ] 连续编辑至少 30 分钟
- [ ] 默认 flags LF/CRLF/Mixed/BOM 零编辑无 dirty/save/mtime/hash 变化
- [ ] 与 legacy 的阻塞体验差异已记录
- [ ] 每项 minimum parity 已填 PASS/FALLBACK-ACCEPTED/FAIL，并附 fallback 证据

人工结论：NOT STARTED

## Reviewer 与决定

- [ ] 默认保存消费者完整审计
- [ ] command 局部 range
- [ ] resource failure compensation
- [ ] single CM History owner
- [ ] default minimum parity 与 P4B 延后项边界可接受
- Reviewer：NOT RECORDED
- Program Go/No-Go：NOT STARTED