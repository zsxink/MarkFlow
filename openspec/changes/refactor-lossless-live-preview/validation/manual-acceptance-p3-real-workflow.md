# P3 5.11 真实日常工作流证据收集清单（Program Owner）

> 任务 5.11 原文：「收集一轮真实日常工作流证据与 legacy 回退原因；任何数据完整性
> 问题立即关闭默认 flag」。本清单是 5.11 专项——目标不是重复 `manual-acceptance-p3-checklist.md`
> 的 12 节功能验收，而是**按一天真实使用的叙事顺序**走完一条完整工作流并留下证据
> （文件、hash、截图、异常记录），同时明确记录「是否需要回退 legacy 及原因」。
>
> - 候选：commit `90081fc`（或签署时的最新 P3 候选），flags 默认
>   （`losslessCoreSession`=ON、`codemirrorLivePreview`=ON，均不设 localStorage 覆盖）。
> - 环境：`npm run tauri dev` 或已构建 debug 应用；autosave 产品默认（开，10000ms）。
> - 文档：使用 `/Users/xian/markflow-test` 下的真实样本（只读复制到工作区，不改写原样本）。
> - 记录方式：每步在「证据」栏填写实际观察（必要时报存文件 hash/截图）；任何一步出现
>   **打开即 dirty、零编辑写盘、内容丢失/改变、保存后 hash 与预期不符**——立即停止，
>   按 §4 的回退开关关闭默认 flag，并把现象记入 §5，不得继续签署。

## 0. 开始前快照

- [ ] 记录候选 commit：____________
- [ ] 记录 OS/输入法/WebKit 版本：____________
- [ ] 记录将使用的 3 个真实文档（文件名 + SHA-256，`shasum -a 256`）：
  - A（日常笔记，含中文/列表/引用）：____________
  - B（含代码围栏/表格/frontmatter 等 fallback construct）：____________
  - C（含图片 literal）：____________

## 1. 打开（真实日起点）

- [ ] 启动应用，从文件树打开 A：打开即 `dirty=false`（状态栏无未保存标记）
- 证据：____________

## 2. 编辑（真实输入会话 10–15 分钟，中英混排 + emoji + IME）

- [ ] 连续真实输入：中文段落、列表增删、引用、改标题级别、行内代码
- [ ] 期间 IME 组合词（如拼音选字）无丢字、无光标跳变
- [ ] 每隔几分钟 Ctrl+S 一次；autosave 到期自动保存正常
- [ ] 期间 Undo/Redo（Cmd+Z / Cmd+Shift+Z）符合预期，跨 Source/Live Preview 切换后 History 连续
- 证据（异常点记录）：____________

## 3. 保存与重开（字节级抽查）

- [ ] 对 A 保存后退出重开：正文一致；尾部空行数量与编辑前一致
- [ ] 对 A 记录保存后 SHA-256，与「期望」（编辑意图手工推导的文本）比对一致
- 证据（hash）：____________

## 4. 模式切换 / autosave / 外部修改（混合干扰）

- [ ] 在 B 上 Source↔Live Preview 连续切换 20 次：dirty 不变、无写盘（对比 mtime）
- [ ] B 保持打开时用外部编辑器修改并保存：应用出现外部修改提示，三选一（保留当前/加载磁盘/另存为）行为正确
- [ ] 删除 B（外部）：dirty 时弹「重新保存/删除掉」；重新保存后文件内容与编辑器一致
- 证据：____________

## 5. 导出 / Save As / A-B 并行（收尾）

- [ ] A 导出 PDF/HTML：输出与当前内容一致，导出后编辑器内容与 dirty 不变
- [ ] B Save As 到新路径：原文件不动，新文件打开且 dirty=false
- [ ] A、B 同时打开交替编辑：互不影响 dirty/保存
- 证据：____________

## 6. legacy 回退原因记录（5.11 明确要求）

- [ ] 本轮工作流中是否有任何一步让你想切回 legacy（体验/功能/数据担忧）？
  - 无 → 勾选并写「无」
  - 有 → 写明步骤、现象、严重度（阻塞 / 非阻塞）：
    ____________
- [ ] 未出现数据完整性问题（打开即 dirty / 零编辑写盘 / 内容丢失 / serializer 保存）
- [ ] （若出现任一数据完整性问题）已执行回退：
  `localStorage['markflow.losslessCoreSession']='0'` + 重启，并确认旧路径可用

## 7. 结论（由 Program Owner 填写）

- 5.11 真实工作流证据：**COLLECTED / REJECTED**
- legacy 回退原因：无 / 有（见 §6）
- 备注：____________

验收人：____________  日期：____________
