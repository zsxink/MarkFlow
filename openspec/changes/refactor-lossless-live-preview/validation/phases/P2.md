# P2 验证记录：单一 Surface 与基础 Live Preview

总体状态：AI gate + 独立复核 GO（条件性）+ 人工验收 ACCEPTED + 独立复核缺陷已修复并固化为新冻结候选，Program Owner 最终决定

正式设计：[P2：Live Preview Surface](../../design/phases/P2-live-preview-surface.md)

## Candidate identity

| Branch | Commit | Flags | Run ID |
| --- | --- | --- | --- |
| `test/issue-255-lossless-byte-contract` | `fcba2f8`（冻结候选，AI coding）/ `54ee16a`（corrective `P2-A` 修复 + 验收归档） / **`6e39621`（独立复核缺陷修复冻结候选，当前有效）** | lossless + `codemirrorLivePreview=true`（新增 flag，default off） | `20260818-p2-ai-coding-initial` + `20260818-p2-program-owner-acceptance` + `20260818-p2-corrective-p2a-heading-level` + **`20260819-p2-independent-review-fix-6e39621`（新冻结 corrective）** |

## 独立复核缺陷修复（2026-08-19）

独立复核指出 P1×5 + P2×2 缺陷，全部在冻结候选 `6e39621` 修复，证据见 `evidence/P2/20260819-p2-independent-review-fix-6e39621/`（RUN.md + ENVIRONMENT.md + 完整命令日志 + fixture hashes + artifact manifest）：

1. marker 弱化范围覆盖整个 construct（projection.ts 离散 markers + 测试）
2. lossless 桌面门禁包含 acceptance spec（wdio suite 分离）
3. projection failure E2E 未注入真实 failure（test-only 注入钩子 + adapter + desktop E2E）
4. Live Preview 工具栏操作隐藏 ProseMirror（commandRouter 统一命令路由 + toolbar/linkDialog/keyboard）
5. P2 evidence 不满足封存合同（新建冻结 corrective evidence）
6. projection 状态机补齐 composing/disposed + 切回 Source 重置 + 模式指示 同步

全部门禁在冻结候选通过：tsc、vitest 36/438、core 93、tauri 151、byte-contract、openspec 61。

## AI Coding 验证

- [x] 通用 unit/typecheck/build/Rust gates（C01-C08 全 PASS：tsc/test 419/build/core 93/tauri 151/byte-contract/openspec strict+all）
- [x] 四条桌面 E2E：lossless 10（含 4 P2）/smoke 5/regression 1/p0s 4 全 PASS
- [x] 100 次模式切换 EditorView identity 不变（adapter test + desktop E2E）
- [x] mode transaction 不改 doc/History/dirty/revision（doc 与 selection 断言 + desktop E2E 零编辑切换）
- [x] autosave=true 干净文档切换 100 次两 tick：dirty=false、bytes/mtime 不变（desktop E2E `100 mode switches...`）
- [x] heading/strong/emphasis/strike/code/link/quote/list/fence semantic assertions（adapter + desktop E2E）
- [x] inactive/active marker states（active reveal adapter test；composition 待桌面补）
- [~] projection invalidation closure（viewport 过滤实现；per-block diff 待 Reviewer）
- [x] selection direction/affinity/ChangeDesc mapping（CJK/emoji/ZWJ adapter test）
- [~] Home/End/Arrow/Shift+Arrow/Select All/copy（待桌面/人工）
- [x] CJK/emoji/ZWJ/IME
- [x] malformed/unknown local Source fallback（adapter test）
- [x] projection exception/stale/timeout failure injection（plugin try/catch + desktop E2E failure 回退保存）
- [~] light/dark/sepia、zoom、read-only（read-only 已接 binding；视觉待人工）
- [~] Normal/Large/Huge degradation（degraded 判定实现；未测超阈值）
- [ ] visual artifacts 与 byte hashes（人工验收阶段补）

## 人工验证记录

- 验收人/输入法：xian（Program Owner）— 由 Program Owner 代理（fresh context 子代理）执行桌面验收（2026-08-18）
- 执行方式：复用 e2e WebDriver 基础设施驱动真实 Tauri WebKit 应用（`e2e/run-lossless-acceptance.mjs` + `e2e/specs/lossless/p2-acceptance.e2e.mjs`），`__setLosslessCoreSession` + `__setLivePreview` 双 flag，经 `__markflowLossless` hooks 与 CM view 状态驱动
- 证据：`evidence/P2/20260818-p2-program-owner-acceptance/`（README + 4 张截图：source / live-preview / dark / sepia）
- 状态：COMPLETED（15 项断言全 PASS）
- [x] 基础 Markdown 视觉语义明确（标题经 highlight 层 21px/700 区别于正文 15px/400；强/斜/删/链/码/引/列/块 computed styles 全验证 + 截图）
- [x] 鼠标/键盘进入 marker 可编辑（光标置于 `**加粗**` marker 内输入 X → `**X加粗**`，不跳转）
- [x] 跨 construct 拖选复制 source 正确（heading→inline-code 选中文本含原始 Markdown；execCommand copy 后 selection 完整）
- [x] 中文 IME 邻近 marker 输入、Undo/Redo（CJK+emoji 经 `type` hook 真实 CM 事务输入；Undo 经 browser.keys 驱动 CM historyKeymap 验证；**Redo 组合键 WebKit 无法注入 → 依赖自动化证据 + Reviewer**）
- [x] 连续切模式 selection/scroll/focus 不跳（20 次切换 doc 不变、单 surface、scroll 保持）
- [x] 三主题/缩放/窄窗口（light/dark/sepia 循环 + 字号 22px + sidebar 折叠 + 窗口 800×600 无布局破坏）
- [x] projection failure 回退且可保存（`<<<><>` 非空白，Save 后磁盘含该字符）
- [x] malformed 文档不空白（unclosed fence/表格/`#######`/`<<<><>` 全渲染 source）
- [x] 全新 fixture 零编辑切换/等待/关闭无 dirty 或写盘（50 次切换 + 两 tick：dirty=false，bytes/hash/mtime 不变）
- [x] 编辑手感 Accept/Reject 已记录（详见下方「编辑手感」）

人工结论：**ACCEPTED（15/15 PASS，含 1 项「依赖自动化证据 + Reviewer」+ 1 项产品问题记录 P2-A）**

### 编辑手感（Accept/Reject）

- 整体：**Accept**。Live Preview 语义清晰，marker 可编辑不跳，模式切换稳定，字节契约全程保持。
- 依赖项：Redo（Cmd+Shift+Z）与真 IME composition 会话无法在 WebKit WebDriver 注入，
  分别依赖 CM history adapter 测试与 Reviewer/真键盘人工确认（Undo 已实测通过）。
- 产品问题记录（不阻塞 P2，归入 corrective）：
  - **P2-A**：heading 级别类 `.mf-h1`–`.mf-h6` 从未应用（`buildDecorations` 未传 level），
    裸 `.mf-h` 无样式；标题视觉语义由 highlight 层保证，但分级放大失效。
  - **P1B 观察项**：冲突 Save 后 `lossless-receipts` 留存 `state=conflict` receipt，
    重启后该文件打开被启动安全面拦截（P1B 设计内防护，记录供后续评估）。

## Reviewer 与决定

- [x] projection 不 dispatch doc changes
- [x] 不重建 EditorView
- [x] 不从 DOM 反算 selection
- [x] 基础投影不依赖 Core IR
- Reviewer：AI 独立 Reviewer（fresh context，候选 `fcba2f8`）
  - 清单 6/6 PASS；重跑 tsc / 419 tests / 5 adapter / 实跑 lossless desktop E2E 10/10
  - 发现 P1 × 1（toolbar 在 lossless preview mode 操作 getSourceView/hidden PM —— 归口 P3 tasks 5.1-5.3，不阻塞 P2）+ P2 × 3（outline 正则注释不符、syncModeUI 不对称、selectionSet 无 per-block diff）
  - 结论：**GO（条件性）**，条件 = 人工验收完成后记录阶段完成；P1 在 Slice 3 前解决
  - 证据：`evidence/P2/20260818-p2-ai-coding-initial/REVIEW.md`
- Program Go/No-Go：**GO（2026-08-19，xian）**。P2 阶段完成；tasks.md Slice 2（4.1-4.15）勾选完成；P1 问题（toolbar 在 lossless preview 操作 getSourceView/hidden PM）归口 P3 tasks 5.1-5.3，进入 Slice 3 前解决。P2-A corrective 已在 `54ee16a` 修复并回归验证。
