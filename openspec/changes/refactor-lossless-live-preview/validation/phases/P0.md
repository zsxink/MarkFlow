# P0 验证记录：现状基线与 Byte 合同

总体状态：CORRECTIVE AI GATE PASS（corrective run `20260813-005131`）＋ 独立 Reviewer PASS ＋ 完整人工验收完成（2026-08-13；重开/ADR/SHA 确认由验收人委托 AI 执行并保留复核权）。剩余：人工 P0 Accept/Reject 与 Program Go/No-Go 仍 PENDING。

正式设计：[P0：现状基线与 Byte 合同](../../design/phases/P0-baseline-contract.md)

## Candidate identity

| Branch | Commit | Flags | Run ID |
| --- | --- | --- | --- |
| `test/issue-255-lossless-byte-contract` | d6f289e（evidence 提交前） | legacy baseline | `20260812-192707-p0-baseline` |
| `test/issue-255-lossless-byte-contract` | dd5969b（evidence 提交前） | legacy baseline | `20260813-005131-p0-corrective-zeroedit-lifecycle`（corrective） |

## 前置检查

- [x] 环境记录完成（evidence ENVIRONMENT.md）
- [x] fixtures 使用合成数据
- [x] 基线 SHA 与 dirty status 记录
- [x] P0 child Issue/change 已创建（#255 / `p0-lossless-byte-contract`）

## AI Coding 验证

> 下列历史勾选项属于历史 run `20260812-192707-p0-baseline`。该 run 对 serializer/L0/L1
> harness 有效；其遗漏的零编辑生命周期已由 corrective run `20260813-005131` 补齐（下节）。
> 历史 run 不得单独称为完整 P0 AI gate。

- [x] `npm test`（339 passed）
- [x] `npx tsc --noEmit`（0 errors）
- [x] `npm run build`（exit 0）
- [x] `cargo test --manifest-path src-tauri/Cargo.toml`（126 passed，含 4 个 dispatcher contract）
- [x] `npm run test:e2e`（smoke 1 spec/5 tests passed）
- [x] canonical fixture manifest 与 SHA-256，尾部 0/1/2/3 boundaries 分项记录
- [x] L0 positive controls（24/24）
- [x] L1 positive controls（95/95）
- [x] 故意修改未触及 byte 的 negative control 被 harness 拒绝（L0 23/23、L1 93/93）
- [x] 当前 PM 打开→正文编辑→保存→重开能稳定复现尾部空行丢失（characterization 4/4 捕获 byte 级违约）
- [x] LF、CRLF、Mixed、BOM、Unicode 均有证据
- [x] ADR：EOL provenance/inheritance、coordinates、invalid UTF-8、按操作 identity matrix、guarded-write/CAS capability、Save As（child `docs/adr.md`）
- [x] Muya/Vditor 固定 SHA 行为矩阵（child `docs/behavior-matrix-editors.md`）
- [x] ChatGPT Desktop 26.803.61601 黑盒矩阵区分 Observed/Official/Inference（child `docs/behavior-matrix-chatgpt-desktop.md`；Observed 待人工）
- [x] 原始输出保存在 `evidence/P0/20260812-192707-p0-baseline/`
- [x] AI 结论写入 run record
- [x] 新建 corrective run-id，保持历史 RUN/ENVIRONMENT/REVIEW 不可变（`20260813-005131-p0-corrective-zeroedit-lifecycle`；历史 `20260812-192707-p0-baseline` 未改动）
- [x] 真实 `openFileInEditor → setReadOnly/onUpdate → autosave → saveActiveDocument` lifecycle characterization（corrective run `20260813-005131`；`legacy-open-autosave.characterization.test.ts`，5/5 捕获零编辑写盘违约）
- [x] autosave 使用产品实际启用配置，零编辑等待至少两个 tick（断言产品默认 `autosave=true`/`interval=10000`；驱动真实 `runAutoSaveTick` 两个 tick）
- [x] 记录 dirty transition、关闭提示、save count、mtime/hash/length 与 soft-break/EOL/tail 分项 diff（见 corrective RUN.md 与 `docs/lifecycle-open-autosave-characterization.md`；保存后 SHA 与人工实测一致）
- [x] 证明 standalone Editor、oracle self-check 与 `autosave=false` smoke 不替代真实 lifecycle evidence（见 lifecycle characterization 文档 §替代性证明）
- [x] 重跑默认 gates、两类 characterization 与 OpenSpec strict validation（corrective RUN.md C01–C11 全部 exit 0）

## 人工验证记录

- 验收人：xian（人工执行）
- 日期/设备：2026-08-12 / macOS Darwin 25.5.0（Tauri dev build，DEBUG）
- 使用 fixture/hash：副本在 `/Users/xian/markflow-test/manual-acceptance-20260812/`，原件未触碰（详见 child `docs/manual-acceptance-checklist.md` 结论记录）

### 操作与发现（验收人「仅打开浏览、零编辑」）

验收人打开 4 个 fixture 副本、未做任何编辑。应用自动将全部 4 个文件改写
（mtime 每 ~10s 变化 = autosave 间隔），关闭窗口时弹出未保存对话框（dirty=true）。
**即：仅打开即触发 dirty + autosave 写盘，无需任何编辑** —— 这是比 gate 声称
（编辑后保存丢尾部）更强的复现。

### 字节级结果

| F | 副本 | 基线 SHA | 保存后 SHA | 基线尾部 | 保存后尾部 | 判定 |
| --- | --- | --- | --- | --- | --- | --- |
| F1 | manual-tail2-lf | bc1b50f4…a0 | 12162470…78 | `\n\n` | `\n\n` | LF 尾保留 ✅；中间软换行 `\n`→` ` ❌ |
| F2 | manual-tail3-lf | 201dc08c…d1 | c6e6dacd…12 | `\n\n\n` | `\n\n\n` | LF 尾保留 ✅；中间软换行 `\n`→` ` ❌ |
| F3 | manual-tail2-crlf | 6928ce65…fb | 9f18b50b…a1 | `\r\n\r\n` | `\n` | CRLF→LF，边界 2→1 ❌ |
| F4 | manual-tail3-crlf | db562fc5…2a | 9f18b50b…a1（与 F3 相同） | `\r\n\r\n\r\n` | `\n` | CRLF→LF，边界 3→1 ❌ |

- [x] 未编辑打开不应被错误标记为"已修复" —— 失败如实确认，未误标
- [x] 正文编辑后确认 2/3-boundary fixtures 的失败 —— 2026-08-13 补充轮完成：L1 B1–B3 ×4（LF/CRLF tail2/tail3）全部按预期复现（`X` 插入 prefixOk；LF 软换行→空格、尾保留；CRLF→LF、尾 2→1/3→1），详见 manual-acceptance-checklist.md
- [x] LF fixture 操作完成（F1/F2）
- [x] CRLF fixture 操作完成（F3/F4）
- [x] 重开 UI 结果与 byte diff 一致 —— 2026-08-13 验收人重开保存后的 l1 副本确认：UI 显示正文为一行（软换行已写盘为空格），与磁盘内容一致
- [x] ADR 行为符合产品预期 —— 2026-08-13 AI 受托审阅 ADR-001~006 PASS（6/6 无矛盾），验收人委托并保留复核权，详见 manual-acceptance-checklist.md「ADR 审阅记录」
- [x] 报告没有混用视觉空白行数与 line-break boundary 数
- [x] AI 报告没有隐藏预期失败 —— gate 声称全部复现，且实测失败范围更广

人工结论（2026-08-12 记录，2026-08-13 补充轮更新）：基线应用 REJECT（byte fidelity 不通过）：
① 仅打开即 autosave 改写；② 中间软换行 `\n`→` `（内容级丢失）；③ CRLF 尾坍缩 2→1、3→1（CRLF→LF）。
历史 P0 gate 准确捕获了部分失败，但遗漏真实零编辑生命周期。
2026-08-13 补充轮已由 corrective AI run + 新独立 Reviewer 闭环（均 PASS）；人工复跑 L0 零编辑（保存后
SHA 与 corrective 逐字节一致）与 L1 正文单字符编辑 B1–B5 ×4（全部按预期复现）；重开 UI 与磁盘一致；
ADR 审阅（AI 受托）PASS；corrective SHA 对照确认（AI 受托）4/4 一致。
**人工验收清单全部完成**（重开/ADR/SHA 确认由验收人委托 AI 执行并保留复核权）。剩余仅最终决定：
人工 P0 Accept/Reject（Accept 仅表示基线刻画准确，不表示 legacy byte fidelity 通过）与 Program Owner Go/No-Go。

## Reviewer 与决定

- Reviewer：历史独立复核 agent（fresh context）+ corrective 独立复核 agent（fresh context）
- Review：历史范围 PASS（2026-08-12；报告在 `evidence/P0/20260812-192707-p0-baseline/REVIEW.md`）
- Corrective review：**PASS**（2026-08-13；报告在 `evidence/P0/20260813-005131-p0-corrective-zeroedit-lifecycle/REVIEW.md`）——确认 lifecycle 测试驱动真实 open/initEditor/onUpdate/dirty-check/autosave-tick/save/write_file 调用链，唯一 mock 为 Tauri IPC 边界且路由到真实文件系统，autosave 未关闭、保存未 mock；重跑 4 条命令 exit 0，保存后 SHA 与人工实测一致
- [x] Reviewer 确认 capability spec、专题设计和 DTO 的 EOL 顺序完全一致（ADR-002 与 design 01 §3 规则 4 逐字一致）
- [x] Reviewer 确认 guarded-write/reconcile 合同没有分离式 identity check（ADR-003/004 与 design 02 §2 一致）
- [x] 新 Reviewer 确认 lifecycle test 使用真实文件、真实 autosave、完整调用链，且没有 mock 掉 write（corrective REVIEW.md）
- Open blocking issues：无（corrective lifecycle 已机器捕获；P0S/人工步骤不在本阶段 AI 范围）
- Program Go/No-Go：NO-GO，直到 corrective AI、新独立 Reviewer（已完成 PASS）、完整人工步骤全部通过
- 决定人/日期：NOT RECORDED
