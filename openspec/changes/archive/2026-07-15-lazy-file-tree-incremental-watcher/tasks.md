## 1. 基线与目录读取协议

- [x] 1.1 添加固定种子的 10k/100k、深层目录和单目录大量文件夹具生成器，并记录现有递归扫描与整树刷新基线
- [x] 1.2 定义浅层目录分页请求/响应类型、稳定排序键、游标与 generation 失效语义
- [x] 1.3 实现 Rust 浅层 `read_dir` 命令，保留工作区边界与符号链接安全检查，并记录扫描耗时和条目数
- [x] 1.4 为分页无重复、目录变化失效、条目上限、排序、隐藏项和符号链接编写 Rust 测试

## 2. 共享忽略规则与设置

- [x] 2.1 扩展设置模型和默认值，支持 `.git`、`node_modules`、`target`、`dist` 默认忽略及用户自定义模式
- [x] 2.2 实现 Rust ignore matcher 快照，使目录读取与 watcher 使用同一过滤结果，并在无效模式时保留上一个有效配置
- [x] 2.3 在设置界面展示和编辑忽略规则、分页上限与自动加载深度，并在保存后触发一次受控树同步
- [x] 2.4 添加默认忽略、自定义覆盖、无效规则及设置迁移/回退测试

## 3. 前端规范化状态与懒加载

- [x] 3.1 在 TypeScript storage 和文件树类型中接入浅层分页 API，定义节点加载状态和继续加载元数据
- [x] 3.2 实现以规范化 path 为 key 的纯状态 reducer，覆盖插入、删除、修改、后代重命名、排序、stale 和 generation 校验
- [x] 3.3 将工作区初始化改为仅加载根目录首批条目，将首次展开改为按需加载直接子项
- [x] 3.4 实现单目录“继续加载”和自动恢复展开深度上限，确保分页合并不重复并保持选中/活动状态
- [x] 3.5 将渲染器改为按状态局部创建、修补和移除 DOM 节点，不再以 DOM 结构反推持久路径状态
- [x] 3.6 为 reducer、懒加载、分页、展开恢复、并发请求去重和加载错误重试添加 Vitest 覆盖

## 4. 有界 watcher 事件管线

- [x] 4.1 定义平台无关的 `TreeEvent`，归一化 notify create/modify/remove、rename from/to/both/other 和批量事件
- [x] 4.2 在 watcher 生产端应用共享 ignore matcher，并用固定容量 channel、非阻塞入队和 overflow 标志替换无界积压路径
- [x] 4.3 实现按 `path + kind` 的合并窗口、可安全抵消规则、rename 配对和批次发送
- [x] 4.4 实现溢出事件去重与最小恢复范围计算，确保同一范围只安排一次受控重扫
- [x] 4.5 添加跨平台事件序列夹具测试，覆盖高频重复写入、批量 Git 操作、不完整 rename、队列饱和和忽略目录风暴

## 5. 前端增量事件与一致性恢复

- [x] 5.1 订阅批量 `TreeEvent` 并通过 reducer 局部应用已加载目录中的创建、修改、删除和重命名
- [x] 5.2 对未加载目录事件仅标记 stale，对未知父节点、generation 冲突和无法配对 rename 重新读取最小已加载祖先
- [x] 5.3 将新建、删除、重命名、拖拽及其他 `refreshFileTree` 调用迁移到统一增量状态入口
- [x] 5.4 保持活动文档、选择、展开路径和重命名后代同步，并添加 DOM/交互回归测试
- [x] 5.5 确认生产代码不再依赖递归整树刷新后移除 `read_dir_recursive` 及兼容开关

## 6. 可观测性、虚拟化与验收

- [x] 6.1 添加结构化指标：已加载节点数、队列当前/峰值长度、溢出次数、增量批次数、局部/根刷新次数及恢复原因
- [x] 6.2 扩展基准覆盖 `node_modules`/`target` 高频写入和 Git checkout/branch switch，并输出可比较的首屏、事件处理和刷新结果
- [x] 6.3 根据基准确定分页、队列、合并窗口和可见 DOM 阈值，记录选择及回归预算
- [x] 6.4 若可见节点超过阈值，实现扁平可见行窗口化及滚动定位；否则记录无需虚拟化的测量结论
- [x] 6.5 验证 10k/100k 场景下首屏只读根目录、单事件不重建整树、队列有界、忽略目录无刷新，并运行 Rust、Vitest 与构建检查
